library(foreign)
maya_transactions <- read.dta("C:/Documents and Settings/Ahlu/My Documents/IFMR/Credit Scoring/Mayavaram transactions.dta")
length(unique(uniquegroup)) #Total number of schemes in transaction file
table(noofticketstaken) #Number of members and tickets taken in transaction file
